# Devlog

## 2026-06-06

- **Account drawer redesign.** Reworked the `AccountMenu` drawer into a proper modern sidebar (cues from the Hub: near-black canvas, bright lime accent, hairline borders, subtle accent-tinted hovers): brand bar + icon close, an identity card with a gradient avatar and inline Log in, iconned nav rows (each with an icon tile, label/hint, and chevron/↗) for Watch + RCL sections, and a bottom-pinned Sign out (red-tinted hover). Flex column with a scrollable middle so it scales on short screens.

- **Favourites are now global.** The home "★ Favourites" view used to just filter the current match-list page, so a match starred from history (`/me`) or any other page never appeared (the empty state even said "No favourites on this page"). Rebuilt it as a global view: `buildFavCards` reads every favourite via `listUserFavorites` (all kinds — tst/fort/tournament/recording), enriches tst/fort cards from the recent list pages then the user's own history, and renders compact openable cards for anything it can't enrich. Tournament/recording favourites link back to their tournament/replay routes.

- **Music synced to GO + randomised.** The crys soundtrack now opens on a random track (shuffle on), defaults to 10% volume, and kicks off the moment the match playhead starts — i.e. as the 3·2·1 countdown lands on GO (`playheadPlaying` prop) — instead of autoplaying on load. Manual pause/play still works after.

- **Noclip saved camera views.** In the free-fly (noclip) camera you can now save the current pose ("Save view" in the top-centre toolbar) and jump back to any saved pose by clicking its numbered chip or pressing the matching number key (1–9). State lives in `PlaybackHub` and is bridged into `FreeCamera` via `captureCue`/`onCaptureCam`/`gotoCam` (with remount guards so re-entering noclip doesn't record/jump spuriously); up to 9 slots, each removable.

- **Music player (crys soundtrack).** New `MusicPlayer` layered over the in-game audio — its own `<audio>` element, independent volume, shuffle/repeat, seek, a track list, an animated equaliser, and a floating now-playing chip when the panel is closed. Toggle via the `music` icon in the theater control bar. Tracks live in `public/music/` (gitignored; deployed to prod via scp) with a committed manifest at `src/components/playback/musicTracks.ts`. A missing file is skipped gracefully.

- **Account drawer.** Replaced the inline `AuthBar` everywhere with a single top-right icon (`AccountMenu`) that opens a portaled right-hand drawer: user/avatar + Log in / Sign out, Watch links (Matches, Tournaments, My matches, Favourites), and external RCL links (Dashboard, Hub, Resource). Portaled to `document.body` so it overlays cleanly from the theater too. Deleted `AuthBar` and its CSS.

- **Theater topbar cleanup.** The reaction + auth controls were crammed inline with the mono debug stats and weren't reliably clickable (`.theater-topbar` is `pointer-events: none`). Split into a right-aligned glass **actions pill** (favourite · up/down · divider · user/sign-out, `pointer-events: auto`) above a small dimmed **tech row** (matchId · cache · logs). Reaction buttons render flat inside the pill instead of pills-within-a-pill.

- **Match history epic (B): authoritative "My matches" at `/me`, deep-linked to playback.** Built entirely inside Watch using its `rcl_db` access + shared Supabase session. `src/lib/history.ts` resolves the viewer the same way as the dashboard's `/api/profile/stats` (Supabase profile `username`/`ingame_email` + `linked_logins`), then queries the canonical `matches`/`teams`/`team_players`/`players` joins.
  - **Key find**: canonical `matches.external_match_id` (with `record_source='tst_api'`) is exactly the 24-hex tronstats matchId Watch plays — so history rows link straight to `/watch/{id}`. Validated the query returns playable ids for a real player.
  - The dashboard's `GET /api/v1/players/{username}/matches` looked promising but is a **passthrough that does not filter by username** (returns the global recent list with null stats) — not used.
  - `/me` page + a "My matches" tab on the selector/tournament navs; each card shows mode/server/"as <name>" and the favourite/vote bar. Handles signed-out, profile-not-set-up, and no-matches states.

- **Preferences epic (A): favourites + thumbs up/down + sort-by-rating, stored in `rcl_db`.** Watch holds only the Supabase anon key + session (browser), so preferences can't live client-side. Pattern (mirrors the dashboard's `/api/profile/stats`): server routes verify the shared Supabase session, then read/write the canonical **`rcl_db`** Postgres with `rcl_app`.
  - **Schema** (`db/migrations/0001_watch_match_reactions.sql`, applied to `rcl_db`): `watch_match_reactions` (profile_id = Supabase auth UUID, no FK — same convention as `linked_logins`; `match_kind` ∈ tst/fort/tournament/recording; `favorite`, `vote ∈ {-1,0,1}`) + `watch_match_rating_totals` aggregate kept current by a trigger. Verified the trigger (votes aggregate, cleanup on delete) and `rcl_app` grants.
  - **Server**: `src/lib/db.ts` (pg pool on `DATABASE_URL`), `src/lib/reactions.ts` (session→profileId, totals/mine reads, upsert), `POST/GET /api/reactions`.
  - **UI**: `ReactionBar` (optimistic favourite + up/down; redirects signed-out users to the dashboard login). Wired onto match cards, tournament cards, recording cards, and the theater topbar. Listings gained a Recent/Top-rated sort + a ★ Favourites filter.
  - **Env**: `DATABASE_URL` added to `/etc/default/rcl-watch` (server-only). Local dev uses an SSH tunnel: `ssh -fNL 5433:localhost:5432 uk`, with `.env.local` pointing `DATABASE_URL` at `localhost:5433`.
  - **Identity note**: kept reactions keyed by Watch's own match ids (tronstats matchId / tournament slug / `slug/file`), not canonical `matches.id` — decoupled and deep-linkable. tst vs fort share the unique tronstats id but use distinct kinds; the `/watch/{id}` link now carries `?mode=fort` so the theater bar matches the listing.
  - **Tested**: GET totals (public), 401 on signed-out POST, 400 on bad input, and a full DB read roundtrip via the tunnel. Authenticated write path validated by logic + verified live on prod with a real shared session.

- **Auth foundation: shared RCL Supabase session (cookies across `.retrocyclesleague.com`).** Ported the auth scaffolding (Supabase SSR client/server/proxy helpers, cookie-domain scoping, `AuthBar`, dashboard login redirect with `returnTo` + `rcl_post_auth_redirect`) into the repo. Watch reuses the dashboard's `sb-*-auth-token` cookies, so a login on any RCL subdomain shows the user here immediately; **Log in** bounces to `retrocyclesleague.com/auth/login?returnTo=…` and back. `AuthBar` shows `profiles.username` (or email) + Sign out, placed on the match selector + tournament listings and the theater topbar (compact).
  - **Reconciliation note:** this scaffolding existed only as *uncommitted* changes on the prod server (`/data/rcl/rcl-watch`, on an older `main`), missing our aarec/fort/camera work. Brought it into the repo so it's version-controlled; the server should now stash its local edits and pull instead of carrying drift.
  - **Next 16 proxy convention:** used `src/proxy.ts` (exporting `proxy`) instead of the deprecated `middleware.ts` — the build emitted the `middleware`→`proxy` deprecation otherwise. The Supabase `updateSession` helper lives in `src/lib/supabase/middleware.ts` (a plain module, not the convention file).
  - **Env:** `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` (public client keys) in `.env.local` (dev) / `/etc/default/rcl-watch` (prod); optional `NEXT_PUBLIC_AUTH_COOKIE_DOMAIN`. Added `@supabase/ssr` + `@supabase/supabase-js`.
  - **Not yet built (next steps of the epic):** per-user match history, and preferences (favourite matches, 👍/👎, sort tournaments by rating) — these need Supabase tables + RLS in the shared project; schema design pending.

## 2026-06-05

- **Renamed the "Follow" camera to "Custom" and labelled its settings.** The camera select now reads "Custom" (it's the AA custom camera), and the Camera panel groups distance/height/pitch/turn-speed under a "Custom camera only" heading so it's clear those don't affect cinematic/POV/noclip (FOV stays global). Internal mode value left as `follow`.
- **Fixed team colours on walls/cycles + grouped the roster by team.** `colorForTeam` looked up `TEAM_COLORS` by `team.toLowerCase()`, so decoded aarec names ("Team blue"/"Team gold") never matched the underscore keys ("team_blue") and fell through to a random per-player hashed HSL — wrong wall/cycle colours and a rainbow roster despite correct team labels. Now normalises spaces→underscores so both tronstats and aarec names resolve to the real team colour. The Players panel now groups cycles under team headers (coloured by team, sorted by score) and drops the redundant per-pill team label.
- **Tournament fort fidelity pass: correct spawns/rounds, real teams, a real scoreboard, and a real event console — all from the server's own console feed (descriptor 8).** Big batch off the post-viewer audit.
  - **Decoded the server console (`sn_consoleOut`, descriptor 8 = `Network::ConsoleMessage`, a single string).** This is the verbatim text the server printed: `X core dumped Y for 2 points.` (kills w/ predator+victim+points), `Team blue was awarded 4 points for conquering Team gold's base.` (conquests), `… for holding the base.` (holds), `X committed suicide.`, and chat. `cycleSync.ts` now decodes it into `consoleSamples` (in stream order via the shared `seq`); `toGridpos.ts` parses each line into a typed `MatchEvent` (`kill`/`suicide`/`conquest`/`hold`/`chat` + round markers) with `team`/`points`/`actor`, tagged with the current round + game time. This replaces the old alive-edge "eliminations" heuristic (which missed ~63% of deaths) with authoritative, complete events.
  - **Tournament scoreboard now shows real scores** (was stuck at 0 — aarec has no tronstats kill/conquer logs). `PlaybackHub.teamScores` detects event-based scoring and accumulates the console's own point awards per team up to the playhead (kill=2, conquer=4, hold=6 — the exact ladle scoring), falling back to the tronstats kills×30 path otherwise.
  - **Fixed wrong fort spawns + off-by-one rounds.** A pre-game fragment was being counted as "round 1" (12 stray samples starting at t≈7.7, no spawn), shifting every real round and showing scattered mid-round positions as the opening. `toGridpos.ts` now drops rounds that captured no spawn (min time > 3s) and have little volume (< 100 samples), then renumbers — real rounds now start at t=0 with the true spawn formation (gold cluster (255,50), blue (245,450)).
  - **Fixed fort team names** ("Empty team"/"spectator"). `current_team_id` is unreliable: blue players point at a stale duplicate team object literally named "Empty team" with a neutral grey colour, while the real "Team blue" object has no players, and some players (e.g. P4) sit on team 0. Authoritative signal: fortress players spawn next to the zone they defend, so `assignTeamsBySpawn` labels each (round, player) by the nearest team-coloured decoded zone (`teamColorName` maps a zone's RGB → "Team blue"/"Team gold"/…). Kill-event teams are reconciled the same way. Result: clean 6-gold / 6-blue split, P4 correctly gold.
  - **Event console UI** now renders all kinds (kill orange, conquest lime, hold cyan, chat white, round accent) with per-kind CSS.
  - **Cache schema bumped `v2`→`v3`** (event/round shape changed). Recording loading copy, the Physics & Zone panel (now notes the manual sumo is overridden when zones are decoded), and the recording zone default (heuristic sumo off for recordings) all corrected.
  - **Deferred (noted, not shipped):** `last_turn_position`-based wall densification — would add minor zigzag detail on big 1 Hz sync gaps, but inserting synthetic turn points with guessed timing risks drawing *wrong* walls; not worth it now that walls render fully.
- **Fixed missing walls on tournament (aarec) recordings.** Walls render from each cycle's position samples, and the trail builder dropped any move whose consecutive samples were >8 units apart — a teleport filter tuned for dense tronstats logs. aarec enemy cycles only sync ~1×/sec (CYCLE_SYNC_INTERVAL_ENEMY 1), so legitimate moves are ~30 units apart and ~45% of segments (the long straight runs that make up most wall length) were discarded, leaving sparse stubs. Replaced the fixed cap with an odometer-relative guard: skip a move only when its straight-line distance exceeds the plausible travelled path (`max(∫speed·dt, peakSpeed·dt)·1.6 + 4`) or the gap is >5s — so real sparse moves are kept (100% retention on the ladle sample, up from 55%) while teleports/respawns/desyncs are still dropped. Render-time only; no cache change.
- **Fortress zones from the recording's network stream + a toggleable event console.** Fort matches showed the TST sumo zone in the wrong spot; the fix recovers the real zones from the aarec stream rather than guessing.
  - **Zone decode (gZone, descriptor 340).** The fort `MAP_FILE` in CONFIG is a zone-less square (`square-1.0.1`) — the fortress zones come from a server-side map override and aren't referenced, and zone syncs only start mid-round so spawn-cluster guessing is unreliable. The faithful source is the `gZone` net object (descriptor 340, present 43× in the sample). Added `ZONE_SYNC` + `FUNCTION` schemas to `cycleSync.ts` (`ZoneV1Sync`: base+create_time / colour+reference_time+pos_x/pos_y/radius/rotation linear functions). `radius(t) = offset + slope·(t − ref)` models **both** fortress (slope 0, fixed) and the sumo win-zone (slope < 0, shrinking) with one model. Validated: 2 zones at exactly (250,50) gold and (250,450) blue, radius 40 — matching the `for_old_clients` fort map.
  - **Pipeline.** `toGridpos.ts` now collapses zone samples to one definition per centre (taking the team colour from the latest non-black sync, since creation messages start black) and emits `zones: DecodedZone[]` + `events: MatchEvent[]`. The aarec API payload changed from a bare `TstGridposLog[]` to `{ logs, zones, events }` (cache filename later bumped to `.v3.json`); `PlaybackHub.loadLogs` accepts either shape (tronstats array vs aarec object).
  - **Render.** `CinematicScene` renders decoded zones (reusing the rotating-ring `SumoZone`, now colour-parameterised) with per-zone team colour and `radius(t)`, taking priority over the heuristic sumo and orbiting the cinematic camera on the zones' centroid.
  - **Event console.** New toggle (console icon, shown only when events exist) opens a top-left feed of the current round's events up to the playhead, auto-scrolling. (Now sourced from the decoded server console — see the top of today's log — covering kills/conquests/holds/suicides/chat, not just derived eliminations.)
- **Fixed `ENOENT … rename …tmp` on tournament load (cache-write race).** The aarec convert cache used a `pid`-only temp filename (`<final>.<pid>.tmp`). Conversion is slow (download + unzip + decode), so two concurrent requests for the same recording — e.g. the client effect double-firing under React StrictMode — run in the *same* Node process, collide on that temp path, and the first to finish renames it away, leaving the second's `rename` to throw ENOENT → "COULD NOT LOAD THIS MATCH". Now each write uses a unique `randomUUID()` temp name; `rename` is atomic so the last writer simply wins. Applied the same fix to the original `tronLogs.ts` (same latent bug, rarely raced).
- **Tournament `.aarec` viewer (browse the armarecordings archive + replay it in the existing hub).** Added a "View Tournament" tab that browses `http://vps-zman.armagetronad.org/~manuel/armarecordings/` and replays its recordings through the same `PlaybackHub`/`CinematicScene`/scoreboard pipeline.
  - **Decoder (`src/lib/aarec/`)** — the hard part. A `.aarec` is a deterministic *replay*, not a position log; the wire format turned out to be Armagetron's legacy **0.2.8 section-based binary serializer**, not protobuf as the plan assumed. Built it in layers: `sections.ts` (plain-text VERSION/CONFIG/T/READ tokeniser) → `packets.ts` (reframe `READ` datagrams into legacy stream messages: `descriptor`/`messageId`/`wordLen` u16-BE + words, sender id in the last 2 bytes) → `stream.ts` (ports the custom serializer: 26-bit-mantissa/6-bit-exponent float, two-word int32, length-prefixed strings, section masks First/ID/Second/All) → `cycleSync.ts` (net-object schemas for descriptor 24 sync, 320 gCycle create, 201 ePlayerNetID, 220 eTeam; tracks object class + cycle→owner) → `toGridpos.ts` (assembles syncs into `TstGridposLog[]`, derives `RoundId` from game-time resets, `ElapsedTime` from sync time, `MAX_COORD=5000` sanity filter, strips `0xRRGGBB` colour codes from names). Verified against a real sample: **24,691 logs, 18 rounds, real player names/teams, sane bounds.**
  - **Index lib (`src/lib/armaRecordings.ts`)** — server-side Apache autoindex HTML parser (`getTournaments`/`getTournamentEntries`) mirroring `tronMatches.ts`: `React.cache` per-request dedupe + TTL disk cache in `.cache/arma-index/`, with `validateSegment` path-traversal guard.
  - **Convert API (`src/app/api/aarec/[...path]/route.ts` + `src/lib/aarecLogs.ts`)** — `/api/aarec/<tournament>/<file>`: fetch the recording, unzip with `fflate` (a multi-`.aarec` zip merges with per-file `RoundId` offset), decode, cache the JSON forever in `.cache/aarec/`. Cache hits stream off disk with a real `Content-Length`/`x-watch-bytes` (exact client progress bar); cold loads are CPU-bound so no mid-decode progress. Verified: cold load 6.36 MB JSON `x-watch-cache: network`, 2nd call `x-watch-cache: cache`.
  - **UI + wiring** — "View Tournament" tab on `page.tsx`; `/tournaments` and `/tournaments/[tournament]` listing pages reusing the `.selector-*`/`.match-grid` styles; `/tournaments/[tournament]/[match]` renders `PlaybackHub` with a new optional `logsUrl` prop pointed at the aarec API (defaults to `/api/logs/<matchId>` otherwise). (The tournament scoreboard/events are now driven by the decoded server console — see the top of the 06-05 log.)
- **Thinner trail walls.** Reduced the wall box depth from 0.14 → 0.06 units so walls read as thin blades rather than chunky slabs.
- **Faithful explosions (1:1 with `gExplosion::Render`).** Replaced the bespoke shell+sphere-flash burst with the game's actual model: `expvec` = 9 fixed cardinal/diagonal rays + 31 random ones with a wide horizontal spread (`fak=7`) and a fixed upward bias, normalised (remapped game z-up → our y-up). Each ray draws from inner radius `e` to outer `a1`, both ×100 units (`EXPLOSION_SPEED`), where `a1` = age in seconds; full opacity for the first second, linear fade to 0 by ~2s, and after 1s the inner ends lift off into an expanding hollow shell — exactly the source formula. Dropped the additive core-flash sphere (that fireball path is commented out in the game). Net: a big spiky ray-burst instead of a small puff.
- **Fixed the dark play button.** `.control-cluster--transport .ctl-btn` (0,2,0) was overriding `.ctl-btn--primary` (0,1,0), so the play button got the dark bezel background while keeping the dark icon colour → invisible. Scoped the primary rules under the transport cluster (`.control-cluster--transport .ctl-btn.ctl-btn--primary`) so the lime gradient wins.
- **Faithful round-start countdown + announcer voices.** Reworked the `3→2→1→GO` to match the game (gGame.cpp, `PREPARE_TIME=4`): one second per count, and "GO" lands exactly when the round starts playing (no held pause). Plays the original AA voice announcer on each beat — `3/2/1voicemale.ogg` + `announcerGO.ogg` (copied into `public/aa/sound`, wired through `useMatchAudio` via a new `countdown` param so it respects the master volume/enable). Removed the old duplicate time-based "GO" trigger. Visually toned it down from a giant flashing lime number to a clean centred number that appears/holds/fades, mirroring the game's `con.CenterDisplay` announcer text.
- **Fixed coplanar-wall z-fighting (the triangular bleed-through when one wall sits directly on another).** The per-player `polygonOffset` used `factor=0` (constant offset only); at the near-edge-on angles these walls are viewed the depth gradient across a polygon is steep, so a constant bias is too small to win the depth tie and they flicker. Now each player's wall uses a *slope-scaled* `polygonOffsetFactor` (= its centred depth rank) plus matching units, so coplanar walls separate cleanly at any viewing angle. Corners stay geometrically exact (no positional nudge).
- **Debug / FPS overlay.** Added a "Show debug overlay (FPS)" toggle in the Camera settings panel. When on, a small top-left HUD shows FPS, lowest FPS seen, frame time (ms), draw calls, triangles, geometries, textures, and shader programs. Implemented as a `DebugStats` component *inside* the Canvas that samples `gl.info` + frame timing in `useFrame` and writes straight into a PlaybackHub-owned DOM node via ref ~4×/sec — so the HUD itself never re-renders the React tree (no measurement overhead/feedback loop). Use it to spot what's driving the perceived slowdown (draw calls / triangle count climbing as walls accumulate is the usual suspect).
- **Control-bar redesign + round countdown + bigger explosions** (UI was a row of random text/emoji buttons):
  - Rebuilt the control bar into three clean clusters — **transport** (rewind-15 / play-pause / forward-15 / auto-advance toggle, with the play button as the lone accent control), centred **selects** (Round/Speed/POV/Camera), and a segmented **panels** group (Scores / Players / Camera / Sound / Physics · Share / Fullscreen / Minimise). All buttons are now consistent single-weight line icons (`Icon`/`IconButton`, Lucide-derived SVGs) with `title`/`aria-label` tooltips instead of scattered words/emoji.
  - **Auto-advance rounds is now the default** (`autoNext` defaults true; toggled via the skip-forward icon in the transport cluster).
  - **3-2-1 round-start countdown**: playback no longer jumps straight in. Starting from the top of a round (play button or Space) runs a `3 → 2 → 1 → GO` overlay (`countdown` state + a setTimeout chain), then begins play; auto-advancing to the next round runs it too. Click the overlay to skip. Pausing mid-round and resuming does *not* re-trigger it (only fires when `time ≤ 0.1`). (Timing/visual/audio later tuned to be game-faithful — see the top of today's log.)
  - **Bigger TST hole explosions**: bumped `maxRadius` (arena×0.14→0.24, clamp 6–16 → 14–30), spark count 44→72, added a bright additive core flash sphere, and roughly doubled the flash pointLight — death/hole blasts now read at TST scale instead of a small puff.
- **Fixed jittery name plates.** `CycleLabel` was soft-following the cycle in its own `useFrame` lerp — a second rAF clock running independently of the `PlaybackHub` rAF that advances `time`. The two loops drift out of phase, so the eased label position beat against the bike's real position every frame and the names shook. Removed the lerp; the label is now positioned directly from the cycle's interpolated world point (same render source as the bike), so it's perfectly steady.
- **Real kill + zone scoring** (fixes scoreboard stuck at 0). The tronstats log stream is polymorphic (`$type`) and we were discarding everything except `TstGridposLog` client-side — so the actual scoring events never reached the UI, and the "rounds won" heuristic read 0 until a round ended. The stream also carries `CycleDestroyLog` (a kill: `Username` victim, `Predator` killer, pos/time) and `ConquerLog` (a zone capture: `Usernames`, `Team`, `Score`, time). `PlaybackHub.loadLogs` now parses both from the raw array: kills are credited to the **predator's** team (resolved via a username→team map from the position logs; suicides/same-team frags dropped), conquers add their `Score` to the capturing team. The scoreboard derives a **live** match total — everything in finished rounds plus current-round events up to the playhead — as `score = kills×30 + zonePoints`, with a per-team `N kills · M zone` breakdown. The `×30` and the components were calibrated against the official `MatchHistory` summary (parsed kills/zone match exactly; team `score` = kills×30 + zone, exact for two teams and within 10pts for the others due to upstream edge-case bonuses). New `TstCycleDestroyLog`/`TstConquerLog` types + `isCycleDestroyLog`/`isConquerLog` guards (match on `$type`) in `tstLog.ts`.
- Audio controls: the speaker button now opens a Sound panel with a master volume slider and per-channel mutes (Cycle engine / Turn sounds / Explosions), plus the overall enable toggle. Implemented in `useMatchAudio` via a master `GainNode` (all sources route through it; volume is set on it) and channel booleans gating the engine gain, turn one-shots, and explosion one-shots.
- Playtest feedback batch:
  - **Cinematic centring**: the world origin is the *player-position bounding-box* centre, which is off from the map/zone centre, so the cinematic orbit looked off-axis. The orbit + lookAt now centre on the zone (when enabled) via a new `orbitCenter` prop.
  - **Faithful win-zone** (gWinZone.cpp Render): replaced the smooth ring/cylinder with a ring of `ZONE_SEGMENTS=11` vertical quads, each spanning `ZONE_SEG_LENGTH=0.5` of its arc (dashed), rotating at `0.3` rad/s, additive blend, height ~4 (game uses 5). Geometry built once and scaled by the live radius; rotation derived from `time` so it stays scrub-safe. Radius shrink stays linear per the map's `growth`.
  - **Sizing**: `CYCLE_SCALE` 0.72 → 0.5 (cycles read ~50% too big); trail walls scaled to 65% height (`WALL_HEIGHT_FACTOR`).
  - **Name plates**: moved out of the marker into their own `CycleLabel` overlay; name is bigger and the `@suffix` is split off small/dimmed.
  - **Camera controls**: new "Camera" settings panel with FOV, camera distance (`back`), camera height (`rise`), and turn-speed sliders (`CameraConfig`), de-cluttering the toolbar. Follow rig reads these.
  - **Lag / freeze-jump**: capped the rAF delta at 0.1s so a janky frame eases through instead of fast-forwarding past the action; added a cheap AABB reject to the per-frame skew raycast (`sensorHit`) so it isn't O(players×walls) every frame.
  - **Scoreboard on Tab**: Tab toggles a team-grouped scoreboard panel that persists (no longer fades with the auto-hiding UI). Shows live per-team alive count and strikes through cycles as they die at the playhead.
  - **Camera ↔ AA custom-camera settings**: the follow camera now implements Armagetron's custom camera (eCamera.cpp) with its real parameters/defaults — `CAMERA_CUSTOM_BACK=30`, `RISE=20`, `PITCH=-0.7`, `TURN_SPEED=40`. The camera heading eases toward the cycle heading at TURN_SPEED (`newdir = dir + cycleDir·turnSpeed·dt`, renormalised), then sits BACK behind / RISE above and looks down by PITCH. Sliders (distance/height/pitch/turn-speed) map 1:1 to those vars, giving the higher, in-game-typical POV.
  - **Shareable link**: "Share" copies a `?round=&t=` deep link to the current moment; the hub seeds round/time from those params on load.

## 2026-06-04

- **Production deploy assets**: Added `output: "standalone"` + `postbuild` static copy, `ops/systemd/rcl-watch.service`, nginx vhosts (`ops/nginx/`), and `docs/DEPLOYMENT_NGINX.md` for `watch.retrocyclesleague.com` on `127.0.0.1:3004`.
- Added wheel spin (gCycle.cpp Render L4467-4494 + `rotate(rotationWheel, 2·speed·dt/r)`): each wheel rotates about its lateral (model y) axis at its hub by `θ = 2·odometer/r` (front r=0.43, rear r=0.73). Exposed the cycle's odometer on `PlayerState.distance` via `headDistanceAtTime` so it's a pure function of playback time (scrub-safe); `CycleModel` keeps refs to the two wheel groups and sets their `rotation.y` in a layout effect.
- Corrected the cycle tilt to match the AA source. The earlier version banked into turns, but `gCycle.cpp`'s "animate skew" (~L3110) is wall-proximity based, not turn based: it casts two sensors 45° forward-left/right, measures distance to the nearest wall on each side (`extension = .25`, clamped), and leans by `skew = clamp(-lr/2, -leftHit·.5, rightHit·.5)` where `lr = (leftHit-rightHit)/extension`, rendered as a roll of `atan(skew)` about the forward axis. So the cycle tilts when grinding close to a wall (max ~±7° given the fac/extension clamp), not when cornering. Reimplemented as `computeLeans` (ray vs trail+rim segments in game coords, steady-state of the ODE so it's scrub-safe) and threaded a per-player `lean` into `CycleMarker`. Note: it's a genuinely subtle, close-contact effect by design — it only fires within ~0.25 game units of a wall.
- Added readable in-scene cycle name labels: each live cycle gets a billboarded drei `<Html>` pill (player colour dot + name) above it, `distanceFactor` 26 so it scales with distance, dark backing + text-shadow for contrast, `pointer-events:none` so it never blocks the canvas; the followed/selected player's label fills with their colour. Fixes not being able to tell which bike is who.
- Playback feedback pass: (1) cycles were oversized — set `CYCLE_SCALE` to `0.5` to match the game's `glScalef(.5,.5,.5)`. (2) Follow camera whipped around on turns — split the rig's lerp responsiveness by mode (pov 8, follow 1.9, cinematic 3.6) so follow eases instead of snapping. (3) Added a universal FOV slider (30–110°) wired into `CinematicScene`'s `PerspectiveCamera` so it applies to every camera mode (incl. noclip, which shares the default cam). (4) Added an "Auto next round" checkbox: end-of-round now just clamps time and a separate effect either advances `roundIndex`/resets time when enabled or stops. (5) Replaced the four native `<select>`s (Round/Speed/POV/Camera) with an RCL-branded `RclSelect` (trigger + upward popover, lime selected state, closes on outside-click/Escape).
- Textured the cycle models + added the turn-bank animation. The `.obj` (and the source `.mod`) ship no UVs — AA generates them via GL texgen at runtime — so we project object-space planar UVs (lateral projection: x→u, z→v over the mesh bbox) onto cloned geometries and map the original `cycle_body.png`/`cycle_wheel.png`. Those textures are greyscale+alpha, so we replicate `gTextureCycle::ProcessImage`: blend the painted detail (alpha) over the player colour in transparent areas (`px = (a*tex + (255-a)*colour) >> 8`), via an offscreen canvas → `CanvasTexture`, cached per colour (wheel darkened ×0.7). Emissive is dropped to a low glow so the texture reads under lighting. Turn animation mirrors the game's `skew`: `trackTurns` finds 90° direction changes + their handedness (`cross = ax*by - ay*bx`), and `leanAt(time)` banks the bike about its forward axis by `LEAN_MAX·exp(-dt/LEAN_TAU)` from the most recent turn — deterministic in playback time so it's scrub-safe (no `useFrame`).
- Replaced the placeholder box/torus cycles with the real AA light-cycle models (`cycle_body/front/rear.obj`, loaded via `OBJLoader` from `three-stdlib`). Assembled with the exact offsets the game uses (gCycle.cpp Render: rear wheel at model `(0,0,0.73)`, front at `(1.84,0,0.43)`); models are x-forward/z-up so the inner group is rotated `-90°` about X into our y-up world. Bodies/wheels get distinct materials; `CYCLE_SCALE` controls overall size.
- Imported the original Armagetron Advanced skin (textures + sounds) from the `armagetronad/` checkout into `public/aa/`. Textures: `floor.png` tiled on the arena floor (every ~20 units), `dir_wall.png` mapped on the cycle trail walls (× per-player instanceColor/vertex tint), `rim_wall.png` tiled on the boundary walls (cloned per wall so each tiles at its own length), and `sky.png` on a big inverted dome (replaced the neon Stars/Sparkles). Note: drei's `useTexture` return is immutable under the React Compiler, so each texture is `.clone()`d in a `useMemo` before setting wrap/repeat/colorSpace (and disposed on unmount). Sounds: new `useMatchAudio` hook plays a looping engine hum while playing (pitched with speed), an explosion as the playhead crosses each genuine death, a turn tick as cycles change direction, the announcer "GO" at round start, and the zone-spawn sting — gated by a new mute toggle (🔊) in the controls. Autoplay is fine because playback starts from a click. The engine uses the Web Audio API (gapless `AudioBufferSourceNode` loop with gain modulation) — an HTML `<audio loop>` of the short `cyclrun.ogg` stuttered/cut out every loop. Turn times are precomputed per cycle (direction-sign changes) and counted per frame via binary search, throttled so simultaneous turns don't flood the mix.
- Added a real progress loading state for the watch page. The logs API route (`/api/logs/[matchId]`) now streams instead of buffering: cache hits stream off disk with a real `Content-Length` (+ `x-watch-bytes`) so the client shows an exact %, and cold loads tee the upstream response to the client while writing a copy to the disk cache (atomic temp→rename), so the browser sees live bytes as they arrive. `PlaybackHub` reads the response body via a reader, decoding chunks and updating a progress bar. Caveat: upstream is `Transfer-Encoding: chunked` with no `Content-Length`, so the *first* (cold) load can only show live MB downloaded with an indeterminate bar — there's no real total until it's cached; every load after that shows an exact %. Validation moved client-side (filter `isTstGridposLog`), which also removes the double parse/serialize the route used to do.
- Fixed "GetLogsForMatch returned logs in an unexpected shape" on real matches. Newer logs come back with `"Id": null`, but `isTstGridposLog` required `Id` to be a string, so every row was filtered out and `parseLogs` threw. `Id` is now typed `string | null` and dropped from the validator; `toSample` synthesises an id (`username@roundId:elapsed`) when it's null. `Id` wasn't used downstream anyway. (Note: the upstream log fetch is genuinely slow — ~18MB / ~30s on a cold match — but it's cached to disk after the first load.)
- Built the home match selector with TST/Fort tabs. Server component reads `?mode=tst|fort` and `?page=` from the URL (tabs/pager are plain links, no client JS), renders match cards (date, rounds, duration, per-team roster + score with the winner highlighted) linking to `/watch/{id}`. New `src/lib/tronMatches.ts` fetches `MatchHistory/Get{Tst,Fort}Matches` and is wrapped in `React.cache` (dedupes within a single render) on top of a 60s TTL file cache at `.cache/match-lists/{mode}-p{page}.json`, with stale-on-error fallback — so upstream is hit at most once per mode/page per minute. The selector intentionally never fetches per-match logs; `GetLogsForMatch` is still only called (and cached) the first time a match is opened. Replaced the old hero landing page + its dead CSS.
- Fixed walls flashing/z-fighting where they run very close together. Two causes: (1) the trail material was `transparent` (opacity 0.95), so the single instanced draw blended overlapping walls in an unstable, camera-dependent order; made it opaque (fade for dying walls already comes from height shrink + colour dim, so no alpha needed). (2) Two walls landing on the same grid line were exactly coplanar; added a tiny deterministic per-player sideways offset (`lateralBias`) so they never share a plane.
- Replaced the positional `lateralBias` hack with a per-player depth bias. Offsetting each segment along its own normal could never give clean corners (the two perpendicular legs always left a step/notch at the join). Now `TrailSegments` groups segments by player and draws one instanced mesh per player, each with a distinct `polygonOffsetUnits` (centred spread, 2-unit gap). Walls stay geometrically exact so corners line up perfectly, and two players' coplanar walls resolve consistently in the depth buffer instead of z-fighting. Trail capacity is now per-player (`player.trails.length`) rather than one big shared buffer.
- Added a "Noclip (free fly)" camera mode: WASD move, E/Q up/down, Shift to sprint, click-drag to look. `FreeCamera` seeds yaw/pitch from the current camera on activation for a smooth handoff and takes over from `CameraRig` while active. Cursor handled via CSS (`.theater-stage.is-noclip`) to satisfy the React Compiler immutability lint (don't mutate `gl.domElement.style`); a hint chip shows the controls in the top bar.
- RCL design pass: rebranded palette (lime `#c6f534` accent + magenta/cyan secondaries on near-black with a faint grid), restyled the fullscreen theater toolbar/panels, and rebuilt the landing page as an RCL "Replay the Grid" card with ghost-outline display type. Removed dead `page.module.css`. Updated `<title>`/metadata to Retrocycles League.
- Added per-side wall tint: the trail box now carries per-face vertex colours (top/sides/ends at different brightness) multiplied by the per-instance player colour, so neighbouring walls stay readable in tight gaps. Note: vertexColors is safe here because the geometry now actually has a `color` attribute.
- Added the shrinking sumo/fortress zone (`gWinZone` / `zShapeCircle`: radius is linear in time). Rendered as a pulsing lime ground ring + faint wall cylinder + floor fill, scaled from unit geometry to avoid per-frame buffer churn. Tunable in the Physics &amp; Zone panel (enable, start radius, shrink/sec, centre X/Y).
- Zone now uses the real TST map values from `public-1.aamap.xml`: `<ShapeCircle radius="50" growth="-0.5"><Point x="60" y="60"/></ShapeCircle>` → `radius(t)=50-0.5t`, centred at map `(60,60)`. Fixes two issues: it was centred on the sample centroid (~59,69) instead of the map point (≈9 units off in Y, pushing players out early) and the previous auto-shrink was too fast.
- Floor grid is now a true 1×1 game-unit grid phase-aligned to integer map coordinates (so the scale is honest), with brighter lines every 10 units for readability.

- Fixed wall-length recession not lining up with real playback: the game measures CYCLE_WALL_LENGTH along the cycle's odometer (`gCycle::GetDistance()` = integral of speed, `distance += speed*dt`), not the geometry of the logged positions. Measured against the logs, the speed integral runs ~40% longer than the reconstructed orthogonal path for every player (under-sampled turns lose geometric distance: RitZ 1915 odo vs 1354 geo, Rudy 2552 vs 1805, rxsyn 653 vs 459 — close to a √2 gap). Switched segment `fromDist`/`toDist` to integrated speed (a move's odometer span distributed across its reconstructed legs by geometry), so the 400-unit window now recedes in sync with the game.
- Fixed wall colours: walls were originally all cyan-white because the instanced material had a constant `emissive="#67f7ff"` overriding each player's instance colour. Switched to a flat `meshBasicMaterial` driven by `instanceColor` (`setColorAt`) so every wall is its player's colour like the real game. Important gotcha: do NOT set `vertexColors` on the material — there is no per-vertex colour attribute, so the shader reads it as black and multiplies the instance colour to black (this briefly turned all walls black). Per-instance tint works via `USE_INSTANCING_COLOR` without `vertexColors`. Fade intensity now dims the colour directly.
- Fixed cycles not sitting at the head of their own wall: `orthogonalPathPoints` had a same-axis branch that ended the reconstructed leg at `[current.x, previous.y]`, dropping the real `current.y` so walls drifted off the logged point. Now the corner is placed by the heading at `previous` and the path always finishes on `current`, so the bike stays exactly on its tail. (Wall length confirmed as CYCLE_WALL_LENGTH 400.)
- Implemented Armagetron wall/death physics so the arena stays readable (verified against `armagetronad/src/tron/gExplosion.cpp`, `gWall.cpp`, `gCycle.cpp`):
  - Finite wall length (`CYCLE_WALLS_LENGTH`, RCL server value 400m): trail segments carry cumulative path distance and are clipped to the receding window `[headDist - wallsLength, headDist]`, so the tail of each wall disappears as the cycle moves. The actively-drawn segment now extends to the cycle head.
  - Walls remove 8s after death (`CYCLE_WALLS_STAY_UP_DELAY`, default 8s): each track's death time is its last log; after `deathTime + delay` its walls are dropped, with a 0.4s sink/dim fade beforehand.
  - Explosions: a dead cycle is replaced by a color spark burst (expanding line shell + flash light) that fades over ~2s, mirroring `gExplosion::Render`. Dead cycle markers are no longer drawn (kills the frozen-bike clutter in the center).
  - Added a tunable Physics panel in the toolbar (wall length, walls-stay-up delay) since the server's exact values aren't in the logs; defaults match the game.

- Rebuilt the watch view as a fullscreen "theater": the Three.js stage fills the viewport with a high-z auto-hiding top bar and a YouTube-style bottom toolbar (scrubber, transport, round/speed/POV/camera selects, players panel, fullscreen, minimise). Controls fade out after ~2.8s of idle while playing and restore on pointer/key activity; a minimise button collapses the bar to a small restore chevron. Added keyboard shortcuts (space/k play, arrows seek, f fullscreen) and the browser Fullscreen API.
- Fixed bikes freezing ~20s into an 85s round: `buildRoundTracks` was inferring death from reconstructed perpendicular wall intersections and truncating every track on false positives. Removed the global wall-timeline/`stoppedAt` clipping pass (and its now-dead intersection helpers); each bike now lives for its full logged sample lifetime, which is the real death signal in the data.
- Kept the orthogonal (90-degree) corner reconstruction between sparse logs for trails — this is the "simulate the green corner between two red logs" handling and is what makes paths read as in-game movement.
- Fixed trail artifacts by snapping playback interpolation and trail generation to orthogonal 90-degree legs, and improved FPS by precomputing round trail segments and drawing them through one instanced Three.js mesh.
- Added a `Makefile` with practical dev/release targets for install, dev server, lint/build checks, release-checks, cache cleanup, and API/page smoke tests.
- Verified `npm run lint`, `npm run build`, cached API behavior, and the watch route smoke response. First API verification wrote `.cache/match-logs/69b85ba1c5015455ee9b0412.json`; later requests returned `x-watch-cache: cache`.
- Moved log loading from the dynamic server page into the client playback shell so the 24 MB match JSON is fetched through the cached API instead of being embedded into the HTML response.
- Implemented the cached match-log API route, shared server cache helper, playback normalization, watch page, transport controls, POV controls, and first Three.js cinematic arena.
- Scaffolded a fresh Next.js TypeScript app in an empty `watch` workspace and added Three.js/R3F dependencies for the cinematic playback viewer.
- Created the initial documentation map and devlog because `docs/INDEX.md` did not exist in the greenfield repo.
- Issue encountered: zsh treated `[matchId]` route directories as globs during setup; quoted those paths when creating dynamic route folders.
